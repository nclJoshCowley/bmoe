# Get Human Readable Names

Helper to access names from data (`y`, `x`, `k`) to be displayed to
users.

## Usage

``` r
get_names_from_bmoe_fit(object)
```

## Arguments

- object:

  Object that inherits from
  [`bmoe_fit`](https://ncljoshcowley.github.io/bmoe/reference/bmoe.md).
