# Multinomial Logit Simulation

Multinomial Logit Simulation

## Usage

``` r
simulate_multilogit(x_wt, wt)
```

## Arguments

- x_wt:

  matrix (`n` by `p_w`). Design matrix to be used in weighting.

- wt:

  matrix (`p_w` by `k`). Weighting regression matrix.
