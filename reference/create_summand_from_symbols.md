# Create Call From (List of) Symbols

Output will be `expression(..1 + ..2)` based on input `list(..1, ..2)`.

## Usage

``` r
create_summand_from_symbols(x)
```

## Arguments

- x:

  list. Expressions to be added together.
